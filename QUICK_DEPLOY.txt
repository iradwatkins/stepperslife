QUICK DEPLOYMENT COMMAND
=======================

Copy and paste this entire command to deploy:

ssh root@72.60.28.175 'cd /opt && rm -rf stepperslife && git clone https://github.com/iradwatkins/stepperslife.git && cd stepperslife && chmod +x DEPLOY_FINAL.sh && ./DEPLOY_FINAL.sh'

After deployment completes, test with:

curl -s https://stepperslife.com/api/auth/providers | jq '.providers.google'

Should show:
{
  "configured": true,
  "status": "ready"
}